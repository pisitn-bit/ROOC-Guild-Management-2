# คู่มือการใช้งานระบบจัดการกิลด์ Ragnarok Origin Classic
## Guild Manager Platform - Step by Step Guide

---

## 📋 สารบัญ

1. [ภาพรวมโครงการ](#ภาพรวมโครงการ)
2. [ข้อกำหนดเบื้องต้น](#ข้อกำหนดเบื้องต้น)
3. [การติดตั้งระบบ](#การติดตั้งระบบ)
4. [การตั้งค่า Firebase](#การตั้งค่า-firebase)
5. [การตั้งค่า Discord Bot](#การตั้งค่า-discord-bot)
6. [การเริ่มใช้งานระบบ](#การเริ่มใช้งานระบบ)
7. [คู่มือการใช้งานแต่ละฟีเจอร์](#คู่มือการใช้งานแต่ละฟีเจอร์)
8. [การนำขึ้นเซิร์ฟเวอร์ (Deployment)](#การนำขึ้นเซิร์ฟเวอร์-deployment)
9. [การแก้ไขปัญหา (Troubleshooting)](#การแก้ไขปัญหา-troubleshooting)

---

## 📖 ภาพรวมโครงการ

ระบบจัดการกิลด์ Ragnarok Origin Classic เป็นแพลตฟอร์มเว็บแอปพลิเคชันที่ช่วยให้หัวหน้ากิลด์และสมาชิกสามารถจัดการกิจกรรมกิลด์ได้อย่างมีประสิทธิภาพ ประกอบด้วยฟีเจอร์หลักดังนี้:

- **📊 Dashboard** - ภาพรวมสถิติและข้อมูลกิลด์
- **⚔️ กิจกรรมกิลด์** - จัดการกิจกรรมกิลด์วอร์และเช็คอินสมาชิก
- **🎰 วงล้อประมูล** - ระบบประมูลไอเทมและวงล้อสุ่มรางวัล
- **👥 สมาชิกกิลด์** - จัดการข้อมูลสมาชิกและสิทธิ์การเข้าถึง
- **🔔 ตั้งค่า Discord** - เชื่อมต่อบอท Discord สำหรับแจ้งเตือนและเช็คอิน
- **⚙️ ตั้งค่าระบบ** - ตั้งค่าทั่วไปของระบบ

---

## 📦 ข้อกำหนดเบื้องต้น

### ซอฟต์แวร์ที่ต้องมี:

1. **Node.js** (เวอร์ชัน 18 ขึ้นไป)
   - ดาวน์โหลดได้ที่: https://nodejs.org/
   - ตรวจสอบเวอร์ชัน: `node --version`

2. **npm** หรือ **bun** (Package Manager)
   - npm มีมากับ Node.js โดยอัตโนมัติ
   - หรือใช้ bun สำหรับความเร็วในการติดตั้ง

3. **Git** (ถ้าต้องการ clone จาก repository)
   - ดาวน์โหลดได้ที่: https://git-scm.com/

### บัญชีบริการที่ต้องมี:

1. **Firebase Project** (สำหรับฐานข้อมูล)
   - สมัครฟรีที่: https://firebase.google.com/

2. **Discord Developer Portal** (สำหรับบอท)
   - เข้าได้ที่: https://discord.com/developers/applications

---

## 🚀 การติดตั้งระบบ

### Step 1: Clone หรือดาวน์โหลดโปรเจกต์

```bash
# ถ้าใช้ Git
git clone <repository-url>
cd ragnarok-origin-guild-manager

# หรือดาวน์โหลดไฟล์ zip แล้วแตกไฟล์
```

### Step 2: ติดตั้ง Dependencies

```bash
# ใช้ npm
npm install

# หรือใช้ bun (เร็วกว่า)
bun install
```

### Step 3: สร้างไฟล์ Environment Variables

สร้างไฟล์ `.env` ในโฟลเดอร์หลักของโปรเจกต์:

```env
# Firebase Configuration
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=your-service-account-email
FIREBASE_PRIVATE_KEY=your-private-key

# Discord Bot Configuration
DISCORD_BOT_TOKEN=your-bot-token
DISCORD_PUBLIC_KEY=your-public-key
DISCORD_GUILD_ID=your-guild-id

# Gemini API (ถ้าใช้)
GEMINI_API_KEY=your-gemini-api-key

# Server Configuration
PORT=3000
NODE_ENV=development
```

**หมายเหตุ:** ดูตัวอย่างได้จากไฟล์ `.env.example`

---

## 🔥 การตั้งค่า Firebase

### Step 1: สร้าง Firebase Project

1. เข้าไปที่ https://console.firebase.google.com/
2. คลิก **"Add project"** และตั้งชื่อโปรเจกต์
3. ข้ามขั้นตอน Google Analytics (ถ้าไม่ต้องการ)

### Step 2: เปิดใช้งาน Firestore Database

1. ในเมนูซ้าย เลือก **"Build"** → **"Firestore Database"**
2. คลิก **"Create database"**
3. เลือกโหมด **"Start in Test mode"** (สำหรับการพัฒนา)
4. เลือก Location (เช่น `asia-southeast1`)

### Step 3: สร้าง Service Account

1. ไปที่ **Project Settings** (รูปฟันเฟือง)
2. เลือกแท็บ **"Service accounts"**
3. คลิก **"Generate new private key"**
4. บันทึกไฟล์ JSON ที่ดาวน์โหลดมา

### Step 4: นำค่ามาใส่ใน .env

เปิดไฟล์ Service Account JSON ที่ดาวน์โหลด แล้วนำค่าต่อไปนี้มาใส่ใน `.env`:

```env
FIREBASE_PROJECT_ID=project-id-from-json
FIREBASE_CLIENT_EMAIL=client-email-from-json
FIREBASE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nprivate-key-content\n-----END PRIVATE KEY-----\n
```

**⚠️ สำคัญ:** สำหรับ `FIREBASE_PRIVATE_KEY` ต้องแปลง `\n` เป็น newline จริง หรือใช้ `\n` ใน string

---

## 🤖 การตั้งค่า Discord Bot

### Step 1: สร้าง Discord Application

1. เข้าไปที่ https://discord.com/developers/applications
2. คลิก **"New Application"** และตั้งชื่อ (เช่น "RO Guild Bot")
3. คลิก **"Create"**

### Step 2: สร้าง Bot User

1. ในเมนูซ้าย เลือก **"Bot"**
2. คลิก **"Add Bot"** และยืนยัน
3. คัดลอก **Bot Token** มาเก็บไว้ (นี่คือ `DISCORD_BOT_TOKEN`)

### Step 3: เปิดใช้งาน Intents

ในหน้า Bot Settings:
1. เลื่อนลงไปที่ **"Privileged Gateway Intents"**
2. เปิดใช้งาน:
   - ✅ **Server Members Intent**
   - ✅ **Message Content Intent**
3. คลิก **"Save Changes"**

### Step 4: สร้าง Public Key (สำหรับ Interactions)

1. ในเมนูซ้าย เลือก **"General Information"**
2. คัดลอก **Public Key** (นี่คือ `DISCORD_PUBLIC_KEY`)

### Step 5: เชิญ Bot เข้า Server

1. ในเมนูซ้าย เลือก **"OAuth2"** → **"URL Generator"**
2. ในส่วน **Scopes** เลือก:
   - ✅ `bot`
   - ✅ `applications.commands`
3. ในส่วน **Bot Permissions** เลือก:
   - ✅ `Send Messages`
   - ✅ `Embed Links`
   - ✅ `Use Application Commands`
   - ✅ `Read Messages/View Channels`
   - ✅ `Add Reactions`
4. คัดลอก URL ที่ generated แล้วเปิดในเบราว์เซอร์
5. เลือก Server และกด **"Authorize"**

### Step 6: หา Guild ID

1. เปิด Discord Settings
2. เปิด **"Advanced"** และเปิด **"Developer Mode"**
3. คลิกขวาที่ Server และเลือก **"Copy ID"** (นี่คือ `DISCORD_GUILD_ID`)

### Step 7: หา Channel ID สำหรับ Check-in

1. คลิกขวาที่ช่องที่ต้องการใช้สำหรับเช็คอิน
2. เลือก **"Copy Channel ID"**

---

## 🎮 การเริ่มใช้งานระบบ

### Step 1: เริ่มเซิร์ฟเวอร์

```bash
# โหมด Development
npm run dev

# หรือ
bun run dev
```

เซิร์ฟเวอร์จะทำงานที่ `http://localhost:3000`

### Step 2: เข้าสู่ระบบครั้งแรก

1. เปิดเบราว์เซอร์ไปที่ `http://localhost:3000`
2. ระบบจะแสดงหน้า Login/Register
3. **สมาชิกใหม่:**
   - เลือกแท็บ "สมัครสมาชิก"
   - กรอกชื่อตัวละคร
   - ตั้ง PIN ส่วนตัว (4-6 ตัวอักษร)
   - เลือกอาชีพ (Job Class)
   - กด "สมัครสมาชิก"

4. **สมาชิกเดิม:**
   - เลือกแท็บ "เข้าสู่ระบบ"
   - เลือกชื่อตัวละครจากรายการ
   - กรอก PIN ส่วนตัว
   - กด "เข้าสู่ระบบ"

### Step 3: ตั้งค่า Admin (ครั้งแรกเท่านั้น)

1. เข้าสู่ระบบด้วยบัญชีแรกที่สร้าง
2. ไปที่แท็บ **"ตั้งค่าระบบ"**
3. กรอก **Admin PIN** (เริ่มต้นคือ `ro-admin-5678`)
4. กด "บันทึกการตั้งค่า"
5. บัญชีนี้จะกลายเป็น Admin ถาวร

---

## 📚 คู่มือการใช้งานแต่ละฟีเจอร์

### 📊 Dashboard

**ฟีเจอร์หลัก:**
- แสดงสถิติสมาชิกทั้งหมด
- แสดงกิจกรรมที่กำลังดำเนินอยู่
- แสดงประวัติการแจ้งเตือนล่าสุด
- แสดงสถานะการเชื่อมต่อ (Online/Offline)

**การใช้งาน:**
- เข้าดูข้อมูลโดยคลิกแท็บ "Dashboard"
- ข้อมูลจะอัปเดตอัตโนมัติทุก 30 วินาที

---

### ⚔️ กิจกรรมกิลด์ (Guild Events)

**สำหรับ Admin:**

**สร้างกิจกรรมใหม่:**
1. คลิกปุ่ม **"สร้างกิจกรรมใหม่"**
2. กรอกข้อมูล:
   - ชื่อกิจกรรม (เช่น "Guild War - 26 ส.ค.")
   - วันที่และเวลา
   - ประเภทกิจกรรม (War/MVP/Event)
   - รายละเอียดเพิ่มเติม
3. กด **"บันทึก"**

**เปิด Check-in:**
1. ในรายการกิจกรรม คลิก **"เปิดเช็คอิน"**
2. ระบบจะสร้าง **รหัสเช็คอิน 6 หลัก** แบบสุ่ม
3. รหัสจะแสดงบนหน้าจอ
4. ถ้าเชื่อมต่อ Discord แล้ว บอทจะส่งข้อความพร้อมปุ่มลงทะเบียนอัตโนมัติ

**ปิดกิจกรรม:**
1. คลิก **"ปิดกิจกรรม"**
2. ระบบจะ:
   - เปลี่ยนสถานะเป็น "Completed"
   - ปิดปุ่มลงทะเบียนใน Discord
   - บันทึกรายชื่อผู้เข้าร่วมทั้งหมด

**สำหรับสมาชิก:**

**ลงทะเบียนผ่านเว็บ:**
1. คลิกปุ่ม **"ลงทะเบียน"** ในกิจกรรมที่ต้องการ
2. เลือกสถานะ:
   - ✅ "มาวอร์" - จะเข้าร่วมกิจกรรม
   - 🚩 "แจ้งลา" - ไม่สามารถเข้าร่วม (ต้องกรอกเหตุผล)
3. กด **"ยืนยัน"**

**ลงทะเบียนผ่าน Discord:**

**วิธีที่ 1: กดปุ่ม**
1. รอบอทส่งข้อความประกาศในช่อง Discord
2. คลิกปุ่ม **"มาวอร์ ⚔️"** หรือ **"แจ้งลา 🚩"**
3. ถ้ากด "มาวอร์" จะมี Modal ให้กรอกรหัสเช็คอิน 6 หลัก
4. กรอกรหัสที่แอดมินประกาศ แล้วกดยืนยัน

**วิธีที่ 2: พิมพ์คำสั่ง**
```
/check 123456
```
(แทนที่ `123456` ด้วยรหัสเช็คอินจริง)

---

### 🎰 วงล้อประมูล (Auctions & Spin Wheel)

**สำหรับ Admin:**

**สร้างไอประมูล:**
1. คลิก **"เพิ่มไอเทมประมูล"**
2. กรอกข้อมูล:
   - ชื่อไอเทม
   - รูปภาพ (ถ้ามี)
   - ราคาเริ่มต้น
   - เวลาปิดประมูล
3. กด **"บันทึก"**

**จัดการประมูล:**
- ดูรายการประมูลทั้งหมด
- ติดตามราคาปัจจุบัน
- ปิดประมูลเมื่อถึงเวลา

**สร้างวงล้อรางวัล:**
1. คลิก **"สร้างวงล้อใหม่"**
2. เพิ่มรางวัล:
   - ชื่อรางวัล
   - จำนวน
   - สีในวงล้อ
3. กด **"บันทึก"**

**สำหรับสมาชิก:**

**ประมูลไอเทม:**
1. คลิกที่ไอเทมที่ต้องการประมูล
2. กรอกราคาที่ต้องการเสนอ
3. กด **"ส่งประมูล"**

**หมุนวงล้อ:**
1. คลิก **"หมุนวงล้อ"**
2. รอระบบสุ่มรางวัล
3. ดูผลลัพธ์และรับรางวัล

---

### 👥 สมาชิกกิลด์ (Members)

**สำหรับ Admin:**

**เพิ่มสมาชิก:**
1. คลิก **"เพิ่มสมาชิก"**
2. กรอกข้อมูล:
   - ชื่อตัวละคร
   - อาชีพ (Job Class)
   - เลเวล
   - Discord ID (ถ้ามี)
3. กด **"บันทึก"**

**แก้ไขข้อมูลสมาชิก:**
1. คลิกที่ชื่อสมาชิกในรายการ
2. แก้ไขข้อมูลที่ต้องการ
3. กด **"บันทึกการแก้ไข"**

**จัดการสิทธิ์:**
- ตั้งค่าให้เป็น Admin
- ระงับบัญชี (Ban/Unban)
- ลบสมาชิกออกจากกิลด์

**สำหรับสมาชิก:**

**ดูข้อมูลส่วนตัว:**
- คลิกที่ชื่อตัวเอง
- ดูประวัติการเข้าร่วมกิจกรรม
- ดูประวัติการได้รับไอเทม

**แก้ไขข้อมูลส่วนตัว:**
1. คลิก **"แก้ไขโปรไฟล์"**
2. แก้ไขข้อมูลที่ต้องการ
3. กด **"บันทึก"**

---

### 🔔 ตั้งค่า Discord (Discord Settings)

**การตั้งค่าพื้นฐาน:**

1. **Webhook URL:**
   - สร้าง Webhook ในช่อง Discord ที่ต้องการ
   - คัดลอก URL มาใส่
   - ใช้สำหรับส่งการแจ้งเตือนทั่วไป

2. **Bot Token:**
   - ใส่ Discord Bot Token ที่ได้จาก Discord Developer Portal
   - ใช้สำหรับบอทแชทและปุ่มกด

3. **Check-in Channel ID:**
   - ใส่ ID ช่องที่จะใช้สำหรับเช็คอินกิลด์วอร์
   - บอทจะส่งข้อความประกาศที่นี่

4. **Bot Name:**
   - ตั้งชื่อที่แสดงในการแจ้งเตือน

5. **เปิด/ปิดการใช้งาน:**
   - เปิดสวิตช์เพื่อเปิดใช้งานฟีเจอร์ Discord

**ทดสอบการเชื่อมต่อ:**
- คลิกปุ่ม **"ทดสอบการเชื่อมต่อ"**
- ระบบจะส่งข้อความทดสอบไปยัง Discord

---

### ⚙️ ตั้งค่าระบบ (Config)

**การตั้งค่าทั่วไป:**

1. **ชื่อกิลด์:**
   - ตั้งชื่อกิลด์ที่จะแสดงบนหน้าเว็บ

2. **System PIN:**
   - PIN สำหรับเข้าสู่โหมด Admin
   - เริ่มต้น: `ro-classic-1234`

3. **Admin PIN:**
   - PIN สำหรับยืนยันตัวตน Admin
   - เริ่มต้น: `ro-admin-5678`

4. **กฎกิลด์ (Guild Guidelines):**
   - กรอกกฎและข้อบังคับของกิลด์
   - จะแสดงในหน้า Dashboard

5. **รีเซ็ตระบบ:**
   - คลิก **"รีเซ็ตข้อมูลทั้งหมด"** เพื่อล้างข้อมูล
   - ⚠️ **คำเตือน:** การกระทำนี้ไม่สามารถย้อนกลับได้

---

## 🌐 การนำขึ้นเซิร์ฟเวอร์ (Deployment)

### วิธีที่ 1: Vercel (แนะนำ)

**Step 1: ติดตั้ง Vercel CLI**
```bash
npm install -g vercel
```

**Step 2: Login**
```bash
vercel login
```

**Step 3: Deploy**
```bash
vercel
```

**Step 4: ตั้งค่า Environment Variables ใน Vercel Dashboard**
1. เข้าไปที่ Vercel Dashboard
2. เลือกโปรเจกต์
3. ไปที่ **Settings** → **Environment Variables**
4. เพิ่มตัวแปรทั้งหมดจากไฟล์ `.env`

**Step 5: Redeploy**
```bash
vercel --prod
```

### วิธีที่ 2: VPS/Cloud Server

**Step 1: Build Project**
```bash
npm run build
```

**Step 2: อัปโหลดไฟล์**
- อัปโหลดโฟลเดอร์ `dist` และไฟล์ที่จำเป็นไปยังเซิร์ฟเวอร์

**Step 3: ติดตั้ง Dependencies บนเซิร์ฟเวอร์**
```bash
npm install --production
```

**Step 4: รันด้วย PM2**
```bash
# ติดตั้ง PM2
npm install -g pm2

# เริ่มเซิร์ฟเวอร์
pm2 start dist/server.cjs --name ro-guild-manager

# ตั้งค่าให้รันอัตโนมัติเมื่อ reboot
pm2 startup
pm2 save
```

**Step 5: ตั้งค่า Nginx (Reverse Proxy)**
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

---

## 🔧 การแก้ไขปัญหา (Troubleshooting)

### ปัญหา: เซิร์ฟเวอร์ไม่ติดตั้ง Dependencies ได้

**วิธีแก้:**
```bash
# ลบ node_modules และ package-lock.json
rm -rf node_modules package-lock.json

# ติดตั้งใหม่
npm install
```

### ปัญหา: Firebase เชื่อมต่อไม่ได้

**วิธีแก้:**
1. ตรวจสอบว่าได้ใส่ค่า Environment Variables ถูกต้อง
2. ตรวจสอบว่า Firestore Database ถูกเปิดใช้งานแล้ว
3. ตรวจสอบ Rules ของ Firestore ให้เป็น Test Mode:
   ```
   rules_version = '2';
   service cloud.firestore {
     match /databases/{database}/documents {
       match /{document=**} {
         allow read, write: if true;
       }
     }
   }
   ```

### ปัญหา: Discord Bot ไม่ตอบสนอง

**วิธีแก้:**
1. ตรวจสอบว่า Bot Token ถูกต้อง
2. ตรวจสอบว่าเปิดใช้งาน Intents ครบถ้วน
3. ตรวจสอบว่า Bot ถูกเชิญเข้า Server แล้ว
4. ตรวจสอบว่า Channel ID ถูกต้อง
5. ดู Log ใน terminal สำหรับ error messages

### ปัญหา: รหัสเช็คอินไม่ทำงาน

**วิธีแก้:**
1. ตรวจสอบว่ากิจกรรมอยู่ในสถานะ `active`
2. ตรวจสอบว่ารหัสที่กรอกตรงกับที่แสดงบนหน้าเว็บ
3. ตรวจสอบว่า Discord ID ของสมาชิกถูกผูกกับตัวละครแล้ว
4. ดู Log ใน terminal สำหรับ error messages

### ปัญหา: ข้อมูลไม่ Sync กับ Firebase

**วิธีแก้:**
1. ตรวจสอบสถานะเครือข่าย (Online/Offline)
2. ตรวจสอบ Console ในเบราว์เซอร์สำหรับ error messages
3. ลองรีเฟรชหน้าเว็บ
4. ตรวจสอบว่า Firebase Project ID ถูกต้อง

### ปัญหา: หน้าเว็บโหลดไม่ได้

**วิธีแก้:**
1. ตรวจสอบว่าเซิร์ฟเวอร์กำลังทำงานอยู่
2. ตรวจสอบ Port 3000 ไม่ถูกใช้งานโดยโปรแกรมอื่น
3. ลองรีสตาร์ทเซิร์ฟเวอร์:
   ```bash
   # หยุดเซิร์ฟเวอร์ (Ctrl+C)
   # เริ่มใหม่
   npm run dev
   ```

---

## 📞 การติดต่อสนับสนุน

หากพบปัญหาหรือมีข้อสงสัยเพิ่มเติม:

1. ตรวจสอบ Log ใน terminal
2. ตรวจสอบ Console ในเบราว์เซอร์ (F12)
3. อ่านคู่มือนี้ให้ครบถ้วน
4. ติดต่อผู้ดูแลระบบ

---

## 📝 บันทึกการอัปเดต

### Version 1.0.0 (26 สิงหาคม 2026)
- เปิดตัวระบบจัดการกิลด์ครั้งแรก
- ฟีเจอร์หลัก: Dashboard, Events, Auctions, Members
- เชื่อมต่อ Discord Bot สำหรับเช็คอิน
- ใช้ Firebase เป็นฐานข้อมูล

---

**© 2026 Ragnarok Origin Classic - Guild Manager Platform**
**เอกสารนี้อัปเดตล่าสุดเมื่อ: 26 สิงหาคม 2026**
